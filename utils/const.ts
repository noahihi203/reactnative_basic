import { StyleSheet } from "react-native"

export const OPENSANS_RENGULAR="OpenSans-Regular"

export const globalStyles = StyleSheet.create({
    globalFont : {
        fontFamily: OPENSANS_RENGULAR
    }
})